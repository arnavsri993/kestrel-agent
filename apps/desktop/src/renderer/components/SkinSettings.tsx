/**
 * Compatibility tombstone for the capability evidence ledger.
 *
 * The desktop renderer no longer exposes or applies skins. Persisted skin IDs
 * and IPC handlers remain outside the renderer for backward compatibility.
 */
export {};
